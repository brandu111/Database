{
  "name": "brandu-tm-database",
  "private": true,
  "version": "1.0.0",
  "description": "BrandU Legal — trade mark portfolio & docketing system (Reva replacement)",
  "workspaces": [
    "packages/shared",
    "server",
    "client"
  ],
  "scripts": {
    "test": "npm run test -w @brandu/shared && npm run test -w @brandu/server",
    "build": "npm run build -w @brandu/shared && npm run build -w @brandu/server && npm run build -w @brandu/client",
    "build:deploy": "npm run build && npm run build:deploy -w @brandu/server",
    "dev": "npm run dev -w @brandu/server & npm run dev -w @brandu/client",
    "seed": "npm run seed -w @brandu/server",
    "start": "npm run start -w @brandu/server"
  }
}
